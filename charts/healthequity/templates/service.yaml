apiVersion: v1
kind: Service
metadata:
  name: {{ template "healthequity.fullname" . }}
  labels:
    app: {{ template "healthequity.name" . }}
    chart: {{ template "healthequity.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "healthequity.name" . }}
    release: {{ .Release.Name }}
